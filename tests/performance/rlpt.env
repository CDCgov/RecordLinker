SEED_FILE=  # A psql data dump file to seed the database
POPULATION_SIZE=100  # Number of synthea patients to generate
SPLIT_RECORDS=1  # 0: 1 API request per patient, 1: 1 API request per patient/encounter
ITERATIONS=2  # Number of times to send the same data to the linkage API
STATE="California"
CITY="Los Angeles"
